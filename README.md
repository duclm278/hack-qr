# Hack QR

- Run module api
  
```shell
cd api
pip install -r requirements.txt
python3 server.py
```

- Run module web

```shell
cd web
python3 -m http.server 5500
```

- Open website: <http://localhost:5500/>
